include "std/io"

for (i:=99; i>0; i--){
  io.println(
    "%{i} bottle%{(i==1)?"":"s"} of beer on the wall, "+
    "%{i} bottle%{(i==1)?"":"s"} of beer.\n"+
    "Take one down, pass it around, "+
    "%{(i==1)?"no more":(i-1)} bottle%{(i==2)?"":"s"} of beer on the wall."
  );
}