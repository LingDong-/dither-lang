include "std/math"
include "std/drw"
include "std/win"

drw.size(480,120);

moux := 0
mouy := 0

while (1){
  drw.circle(moux,mouy,80);
  e := drw.poll();
  if (e.type == win.MOUSE_MOVED){
    moux = e.x;
    mouy = e.y;
  }
}
