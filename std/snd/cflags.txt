: << 'EOF'
IF "%DITHER_SND_USE_DIRECTSOUND%"=="1" (
  set CFLAGS=%CFLAGS% dsound.lib
)
goto :eof
EOF

if [ "$DITHER_SND_USE_COREAUDIO" -eq 1 ]; then
  CFLAGS="$CFLAGS -framework CoreAudio -framework AudioToolbox -framework CoreServices -framework Foundation"
fi
if [ "$DITHER_SND_USE_PORTAUDIO" -eq 1 ]; then
  CFLAGS="$CFLAGS -lportaudio"
fi
